services:
  plex:
    image: plexinc/pms-docker
    container_name: pms
    network_mode: host
    environment:
      - TZ=Europe/Berlin
    volumes:
      - /media/hdd/plex/config:/config
      - /media/hdd/plex/transcode:/transcode
      - /media/hdd/Public/Filme:/data
    restart: always