services:
  ums:
    image: universalmediaserver/ums
    container_name: ums
    network_mode: host
    volumes:
      - /media/hdd/ums-config:/root/.config/UMS
      - /media/hdd/Public/Filme:/root/media
    restart: always